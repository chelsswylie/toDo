import React from "react";
import { FormComponent } from "../organism/form";

interface Props {}

export const FormPage: React.FC<Props> = ({}) => {
  return (
    <div>
      <FormComponent />
    </div>
  );
};
